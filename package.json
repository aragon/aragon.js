{
  "devDependencies": {
    "lerna": "^2.4.0"
  },
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test",
    "install": "npm run bootstrap",
    "bootstrap": "lerna bootstrap"
  }
}
